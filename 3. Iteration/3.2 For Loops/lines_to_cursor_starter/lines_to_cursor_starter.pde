int nLines = 10; 

void setup() {
  size(400, 400);
}

void draw() {
  background(255);
  float spacing = width / ((float) nLines + 1); // what does this line do?
  
  // use a for loop to draw n lines to the cursor
}
