// Look in data folder, decide which pack you want to use

PImage[] images;     
int numFrames; // how many frames are there in the pack?
int currentFrame = 0;
 

void setup() {
  size(800, 400);
  
  // 1. initialize the array with the proper size
  
  // 2. load images into array using a loop

  frameRate(10);  
}

void draw() {
  background(220);
  
  // 3. display the current frame
  
  // 4. advance the frame counter
  
  // 5. if frame counter > total frames, reset to 0
  
  // CHALLENGE: Make your sprite move around
}
